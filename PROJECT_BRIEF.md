# Master 的项目需求

请设计并实现一个简单的 Python ReAct Agent，供命令行和单元测试调用。

产品应能接收一个自然语言问题，在有限步数内反复执行“推理、选择工具、观察结果”，最后返回答案和可审计的步骤轨迹。第一版只需要两个本地工具：一个安全的基础算术工具，以及一个从固定内置知识表查询短文本的工具。不得执行任意 Python、shell 命令或网络请求。

实现需覆盖正常回答、未知工具、非法工具参数、达到最大步数和工具失败等情况。应使用 Python 标准库，提供清晰的模块边界、类型标注、命令行入口、README 示例和可重复的 `unittest` 测试。测试不得依赖网络、用户主目录或全局环境。

Designer 可把工作拆成一个 Mission 和一个足以完成端到端实现的 Task。验收至少应包括完整单元测试、一次命令行冒烟测试，以及确认代码没有 `eval`、`exec` 或 shell 执行路径。

这份文件只是人类提出的原始需求，不是 CHASSISS 受控 artifact。Designer 应以 CLI 返回的模板和校验结果为准。人类代理只接受 CLI 的核验结论，不代替 CLI 修正 artifact 格式，也不直接修复 `.chassis/` 状态。
